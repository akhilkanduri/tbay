[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tbay"
version = "0.2.0"
description = "Execution safety for AI agent tool calls: idempotency, semantic caching, singleflight, policy, approval gating, and a reasoning-linked audit log, as a library, not a service."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "tbay contributors" }]
dependencies = [
    "pyyaml>=6.0",
    "click>=8.1",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.optional-dependencies]
postgres = ["psycopg2-binary>=2.9"]
redis = ["redis>=4.2"]
dev = ["pytest>=7.4", "psycopg2-binary>=2.9", "redis>=4.2"]

[project.scripts]
tbay = "tbay.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
